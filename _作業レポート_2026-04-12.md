# 施工管理ポータルサイトver2 — 整理・リネーム・システムチェック 作業レポート

**実施日時:** 2026-04-12（自動スケジュールタスクにより夜間実行）

---

## 変更前後のファイル構成

### 変更前
```
施工管理ポータルサイトver2/
├── .DS_Store                               ← _削除予定へ移動
├── GAS_タスク管理.js                        ← gas/へ移動・リネーム
├── GAS_工程作成_v1.5.js                    ← gas/へ移動・リネーム
├── GAS_支払い管理_v2.gs                    ← gas/へ移動・リネーム
├── GAS_検査依頼_v2.gs                      ← gas/へ移動・リネーム
├── GAS_現場タスク自動登録_v3.gs            ← gas/へ移動・リネーム
├── assets/
│   └── treeing.png
├── mockup_task_management.html             ← _削除予定へ移動
├── tools/
│   ├── inspection.html
│   ├── payment.html
│   ├── property.html
│   └── schedule.html
├── タスク管理.html                          ← リネーム
├── ダッシュボード.html                      ← リネーム
├── 引継ぎ_新プロジェクト_タスク管理移行.md  ← _削除予定へ移動
└── 支払い管理_Index_v2.html               ← _削除予定へ移動
```

### 変更後
```
施工管理ポータルサイトver2/
├── .gitignore                              ← 新規作成
├── _削除予定/                              ← 新規作成（要確認・削除）
│   ├── README.md
│   ├── .DS_Store
│   ├── mockup_task_management.html
│   ├── 引継ぎ_新プロジェクト_タスク管理移行.md
│   └── 支払い管理_Index_v2.html
├── assets/
│   └── treeing.png
├── gas/                                    ← 新規作成
│   ├── gas-dashboard-data.gs
│   ├── gas-inspection-request.gs
│   ├── gas-payment-management.gs
│   ├── gas-schedule-creation.js
│   └── gas-task-management.js
├── tools/
│   ├── inspection.html
│   ├── payment.html
│   ├── property.html
│   └── schedule.html
├── index.html
└── task-management.html
```

---

## リネーム対応表

| 旧ファイル名 | 新ファイル名 | 備考 |
|---|---|---|
| `ダッシュボード.html` | `index.html` | GitHub Pages のエントリーポイントとして最適 |
| `タスク管理.html` | `task-management.html` | kebab-case |
| `GAS_タスク管理.js` | `gas/gas-task-management.js` | gas/ フォルダに集約 |
| `GAS_工程作成_v1.5.js` | `gas/gas-schedule-creation.js` | gas/ フォルダに集約 |
| `GAS_支払い管理_v2.gs` | `gas/gas-payment-management.gs` | gas/ フォルダに集約 |
| `GAS_検査依頼_v2.gs` | `gas/gas-inspection-request.gs` | gas/ フォルダに集約 |
| `GAS_現場タスク自動登録_v3.gs` | `gas/gas-dashboard-data.gs` | gas/ フォルダに集約 |

tools/ 以下のファイルは既に英語名だったため変更なし。

---

## 更新したリンク・参照

| ファイル | 変更箇所 | 変更内容 |
|---|---|---|
| `index.html` (旧 ダッシュボード.html) | L.5350 `const taskUrl = ...` | `'タスク管理.html'` → `'task-management.html'` |
| `tools/inspection.html` | L.373 戻るボタン | URL-encoded ダッシュボード.html → `'../index.html?tab=tools'` |
| `tools/payment.html` | L.160 戻るボタン | URL-encoded ダッシュボード.html → `'../index.html?tab=tools'` |
| `tools/property.html` | L.89 戻るボタン | URL-encoded ダッシュボード.html → `'../index.html?tab=tools'` |
| `tools/schedule.html` | L.160 戻るボタン | URL-encoded ダッシュボード.html → `'../index.html?tab=tools'` |
| `task-management.html` | L.493 DASHBOARD_URL | `'../index.html'` → `'index.html'` （`../` は同一階層では不要・バグ修正） |

---

## _削除予定フォルダに移動したファイルと理由

詳細は `_削除予定/README.md` を参照。

| ファイル | 理由 |
|---|---|
| `.DS_Store` | macOS自動生成のシステムファイル。GitHubに含める必要なし |
| `mockup_task_management.html` | UIプロトタイプ。どのHTMLからも参照されていないorphanファイル |
| `引継ぎ_新プロジェクト_タスク管理移行.md` | AIチャット間の内部引継ぎメモ。GitHubに含める必要なし |
| `支払い管理_Index_v2.html` | どこからも参照されておらず、tools/payment.html が正式版 |

---

## システムチェック結果

### ✅ 問題なし

| 項目 | 結果 |
|---|---|
| HTML構造（全6ファイル） | 閉じタグ・構文エラーなし |
| JavaScript構文（全6ファイル） | node --check パス（エラーなし） |
| GAS デプロイURL | 全URL メモリの記録と一致 |
| iframeでの直表示（tools/*.html） | payment/schedule/property → iframe表示 OK |
| inspection.html | ネイティブHTMLフォーム（iframeより高品質）、別タブ表示なし |
| エラーハンドリング | index.html: fetch catch 33箇所、task-management.html: try/catch 10箇所以上 |
| console.log 残留 | なし（console.warn のみ、全て意味のある警告） |
| .gitignore | 新規作成済み（.DS_Store、_削除予定/ を除外） |

### ⚠️ 要確認事項（修正は行わず、ユーザーに判断を委ねる）

#### 1. Notion APIキーのハードコード
- **場所:** `gas/gas-task-management.js` L.9
- **内容:** `const NOTION_API_KEY = 'ntn_286809380178...'`
- **リスク:** このファイルをGitHubの**パブリックリポジトリ**にpushすると、APIキーが公開される
- **推奨対応:**
  - リポジトリを**プライベート**に設定する（現在のjognge12-wq/koujikanri-portalの設定を確認）
  - または、GitHubにpushする前にキーをプレースホルダーに差し替え（毎回手動でGASエディタに貼り付ける運用）

#### 2. 業務メールアドレス・氏名のハードコード
- **場所:** `tools/inspection.html` L.82-91
- **内容:** `'城岸 政行'`、`JOUGAN_masayuki@star.sfc.co.jp`
- **リスク:** GitHubに公開すると業務メールアドレスと氏名が外部に公開される
- **推奨対応:** リポジトリをプライベートにするか、プレースホルダーに変更する

#### 3. manifest.json / icon.png が存在しない
- **場所:** `index.html` L.13-14、`tools/*.html` L.8
- **内容:** PWA（ホーム画面追加）用ファイルへの参照があるが実体がない
- **影響:** ホーム画面に追加するとアイコンが表示されないが、通常ブラウザでの動作には影響なし
- **推奨対応:** 必要であれば manifest.json と icon.png を作成する（任意）

---

## 新規作成ファイル

- `.gitignore` — macOS不要ファイル・_削除予定フォルダをGit除外
- `gas/`フォルダ — GASコードを一箇所に集約
- `_削除予定/README.md` — 移動ファイルの理由説明
- `_作業レポート.md` — 本ファイル

---

## GitHub pushの前にやること（チェックリスト）

1. **[ ] リポジトリの公開設定確認** — パブリックの場合はAPIキー・メールを処置する
2. **[ ] _削除予定フォルダの確認・削除** — 内容を確認後、フォルダごと削除してからpush
3. **[x] manifest.json / icon.png の追加** — 2026-04-15 確認時点で存在済み（manifest.json 421B / icon.png 28KB）
4. **[ ] _作業レポート.md の削除** — 内部作業メモなので、push前に削除推奨
5. **[ ] push** — `git add -A && git commit -m "Refactor: rename files to English kebab-case for GitHub"`

---

# 追記：2026-04-15 コード整理・モダン化作業

作業3日目の総合チェックを受けて実施したクリーンアップと改善のログ。

## バックアップ

作業前の状態を `施工管理ポータルサイトver2_backup_2026-04-15/` に完全コピー保存済み。

## 実施した整理作業（デッドコード削除・重複統合）

| # | 作業 | 対象 | 効果 |
|---|---|---|---|
| ① | 旧パスワードオーバーレイ削除 | `index.html` L.2890–2959 | -72行、`checkPassword`の二重定義を解消 |
| ② | PC用サイドバー（未使用）削除 | `index.html` CSS + HTML | -116行、`.sidebar{display:none}`のデッドコードを全削除 |
| ③ | Googleタスク分類ロジック統合 | `index.html` `renderGoogleTasks` / `renderTasksTab` | -23行、`_gtaskBuckets`/`_gtaskItemHtml`/`_gtaskSimpleGroupHtml` ユーティリティに集約 |
| ④ | 曜日配列の一元化 | `index.html` 全体 | 6箇所 → 1箇所（`WEEKDAYS_JP`）。今後の日本語揺れ事故を防止 |
| ⑤ | tools/ 共通CSS/JS抽出 | `tools/*.html` 4ファイル | 新規 `_tool-common.css`（113行）と `_tool-common.js`（12行）に集約。各ファイル -37〜44行 |

**index.html 行数:** 7116 → 6904（-212行、約3%）

## 実施したモダン化作業

（順次追記）

## 現状の「要確認事項」の進捗

| # | 項目 | 状態 |
|---|---|---|
| 1 | Notion APIキー平文（`gas/gas-task-management.js`） | 未処置 — GitHubパブリック公開時は要対応 |
| 2 | 業務メール・氏名ハードコード（`tools/inspection.html`） | 未処置 — 同上 |
| 3 | manifest.json / icon.png 未作成 | ✅ 解決（作成済み） |
| 4 | パスワード平文 `'4022'`（`index.html`） | モダン化作業 I で整理予定 |
